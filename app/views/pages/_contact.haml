%h2 Fale conosco
%br

.center
  %h4
    (12) 3646-1282 /
    %a{:href => 'mailto:pousadasantanna@gmail.com'} pousadasantanna@gmail.com

#form_contact_us
  %h3 Ou nos envie uma mensagem:

  =form_for :message, url: messages_path, method: :post, html: { class: 'form-horizontal'} do |f|
    -@form_fields.each do |fields|
      .control-group
        %label.control-label{ for: "input#{fields[:title]}" }= fields[:title]
        .controls
          -if fields[:type] == :text_field
            = f.text_field fields[:field], placeholder: fields[:placeholder]
          -elsif fields[:type] == :text_area
            = f.text_area fields[:field], rows: 5, placeholder: fields[:placeholder]
    .control-group
      .controls
        %button.btn.btn-primary{type: 'submit'} Enviar
